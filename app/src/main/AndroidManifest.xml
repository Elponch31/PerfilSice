<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="com.example.perfilsice.READ_SICE" android:protectionLevel="normal" />
    <permission android:name="com.example.perfilsice.WRITE_SICE" android:protectionLevel="normal" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PerfilSice">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PerfilSice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.local.provider.SicenetProvider"
            android:authorities="com.example.perfilsice.provider"
            android:exported="true"
            android:readPermission="com.example.perfilsice.READ_SICE"
            android:writePermission="com.example.perfilsice.WRITE_SICE" />

    </application>

</manifest>